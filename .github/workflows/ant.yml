name: POC on Ant based Java project

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: self-hosted

    steps:
    - uses: actions/checkout@v4

    - name: Run mqsiprofile and mqsilist commands
      run: |
        # Source the mqsiprofile
        . "C:\Program Files\IBM\ACE\12.0.7.0\server\bin\mqsiprofile"
        
        # Run mqsilist
        "C:\Program Files\IBM\ACE\12.0.7.0\server\bin\mqsilist"

