name: POC on Ant based Java project
on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: self-hosted

    steps:
    - uses: actions/checkout@v4

    - name: Run Ant Build via Wrapper Script
      run: |
        echo "Starting wrapper script..."
        call C:\krpoc\build_wrapper.bat
